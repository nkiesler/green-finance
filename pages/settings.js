import Layout from '../components/MainLayout';
import { Settings as SettingsForm } from '../components/Settings';

export default function Settings() {
  return (
    <Layout>
      <SettingsForm/>
    </Layout>
  );
}